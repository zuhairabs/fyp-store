import React from 'react';
import {View} from 'react-native';

export default () => (
  <View
    style={{
      flex: 3,
      marginTop: 50,
      marginBottom: 50,
      justifyContent: 'center',
      alignItems: 'center',
    }}>
    <View style={{height: 200}} />
  </View>
);
